This script sends out an ARP request to all clients on the network.
The results will then be printed with the IP and the corresponding MAC addresses.

e.g:

sudo python3 main.py --target 10.0.0.1/24 
